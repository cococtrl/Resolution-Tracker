<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Resolution Tracker</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="nav">
          <a href="/resolutions">All Resolutions</a>
          <a href="/resolutions/new">New Resolution</a>
          <a href="/resolutions/<%=resolution._id%>/edit">Edit This Resolution</a>
            <% if (user) { %>
                <a class="logout" href="/logout">Log Out</a>
            <% } else { %>
                <a href="/auth/google">Login with Google</a>
            <% } %>
      </nav>
<section id="show-page">
    <div><%= resolution.text %></div>
    <form action="/resolutions/<%=resolution._id%>?_method=DELETE" class="delete-form" method="POST">
    <button type="submit">Delete this resolution</button></form>
</section>

<% if (resolution.comments.length) { %>
    <div>Review</div>
      <div>
        <% var total = 0 %>
        <% resolution.comments.forEach(function(r) { %>

        <% }); %>
<div><%= (total / movie.reviews.length).toFixed(1) %></div>
        
        </div>
  <% } else { %>
    <h5>No Comments Yet</h5>
  <% } %>
  
</body>
</html>