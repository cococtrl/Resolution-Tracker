<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Resolution Tracker</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="nav">
        <a class="back" href="/resolutions/<%=resolution._id%>"><span>Back</span></a>
        <a href="/resolutions">All Resolutions</a>
        <a href="/resolutions/new">New Resolution</a>
            <% if (user) { %>
                <a class="logout" href="/logout">Log Out</a>
            <% } else { %>
                <a href="/auth/google">Login with Google</a>
            <% } %>
      </nav>
<section class="edit">
    <h2><%= resolution.text %></h2>
    <form action="/resolutions/<%=resolution._id%>?_method=PUT" method="POST" autocomplete="off">
        <input type="text" name="text" value="<%= resolution.text %>">
        <button type="submit">Save Resolution</button>    
    </form>
</section>
</body>
</html>